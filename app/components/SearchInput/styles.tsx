import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
  },
  input: {
    marginBottom: 20,
    fontSize: 14,
    lineHeight: 19,
    textAlign: "left",
  },
});

export default styles;
